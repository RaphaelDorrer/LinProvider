[project]
name = "linprovider"
version = "0.1.0"
description = "Gets and provides linpeas shell script for HTB Maschines! (I am lazy)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "netifaces>=0.11.0",
    "requests>=2.34.2",
]
